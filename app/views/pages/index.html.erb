<h2> Linksgram</h2>



<ul>
  <% @links.each do |link| %>


  <ul >votes: <%= link.votes.where(:up => true).count  %> <%= link_to "↑", votes_path(:vote => {:link_id => link.id, :up => true}), :method => :post %> || <%= link_to link.title, link.url %> </ul>
  <% end %>
</ul>
<%= will_paginate @links %>

<%= link_to "add link", new_link_path %>